/* The folders are drawn artwork — 122 literals across manila card stock, kraft
 * paper, tab labels and the photographs inside them. Re-pointing an
 * illustration at --fg would not make it brand-correct, it would make it grey.
 *
 * The chrome around the artwork carries no colour of its own, so there is
 * nothing here to re-point.
 */
export default [];
export const allow = [[/#[0-9a-fA-F]{3,8}/, 'drawn artwork: folder stock, tabs and contents']];
