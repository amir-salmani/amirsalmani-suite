/* An algorithm, not a palette: this picks readable ink for whatever colour it
 * is handed, and the two endpoints of that decision are black and white by
 * definition.
 */
export default [];
export const allow = [[/#000000|#ffffff/, 'luminance threshold: the two endpoints of a contrast decision']];
