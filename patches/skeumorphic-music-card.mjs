/* The blue plastic is the thesis. A skeuomorph re-pointed onto two grounds is
 * no longer a skeuomorph, and this item exists to show the technique.
 */
export default [];
export const allow = [[/#[0-9a-fA-F]{3,8}|rgba?\([^)]*\)/, 'skeuomorphic surface: the material is the point']];
