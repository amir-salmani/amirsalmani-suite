/* lucide-react 1.x dropped its brand glyphs — `Twitter` no longer exists, so
 * this component does not build against any current version of its own declared
 * dependency. `Bird` is lucide's own successor shape. Found by verify/, which is
 * the only thing that loads the module.
 */
export default [
  ['Twitter', 'Bird'],
];
