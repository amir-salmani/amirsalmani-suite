/* Six page spreads and two cover stocks. The colours are the book, not the
 * component — a reader flips pages, and pages that are all --fg are a stack of
 * grey rectangles.
 */
export default [];
export const allow = [[/#[0-9a-fA-F]{3,8}/, 'page and cover artwork']];
