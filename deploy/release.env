# Written by .github/workflows/deploy.yml. This file is the deploy trigger —
# changing it is what makes the box rotate. A service that has never deployed
# leaves it empty; reconcile.sh refuses to act on an incomplete pin.
IMAGE=
RELEASE=
SHA256=
